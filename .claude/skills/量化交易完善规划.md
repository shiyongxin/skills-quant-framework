# 量化交易Skills完善规划

## 当前已有功能

### 数据层
- [x] 股票列表获取
- [x] 历史行情数据 (日/周/月K)
- [x] 财务指标获取
- [x] 资金流向数据
- [x] 实时行情
- [x] 批量数据获取

### 指标层
- [x] 基础均线 (MA5/10/20/60)
- [x] MACD (12/26/9)
- [x] RSI (14)
- [x] 布林带 (20, 2)
- [x] 成交量MA

### 策略层
- [x] 价值选股 (PE/PB/ROE)
- [x] 成长选股 (营收/利润增长)
- [x] 技术选股 (金叉/放量)
- [x] 简单回测框架

---

## 一、扩展技术指标库

### 1.1 趋势指标
```python
# 指标分类代码结构
class TrendIndicators:
    @staticmethod
    def ema(df, period):              # 指数移动平均
    @staticmethod
    def dema(df, period):             # 双指数移动平均
    @staticmethod
    def tema(df, period):             # 三重指数移动平均
    @staticmethod
    def tma(df, period):              # 三角移动平均
    @staticmethod
    def vwma(df, period):             # 成交量加权移动平均
    @staticmethod
    def hull_ma(df, period):          # Hull移动平均
    @staticmethod
    def adaptive_ma(df):              # 自适应移动平均
    @staticmethod
    def mcginley_dynamic(df):         # McGinley动态指标

    @staticmethod
    def ichimoku(df):                 # 一目均衡表
        # return: 转换线、基准线、先行带A、先行带B、滞后跨度
```

### 1.2 动量指标
```python
class MomentumIndicators:
    @staticmethod
    def stochastic(df, k=14, d=3):    # KDJ随机指标
    @staticmethod
    def cci(df, period=20):           # 商品通道指标
    @staticmethod
    def williams_r(df, period=14):    # 威廉指标
    @staticmethod
    def momentum(df, period=10):      # 动量指标
    @staticmethod
    def roc(df, period=12):           # 变动率指标
    @staticmethod
    def trix(df, period=14):          # 三重指数平滑平均

    # 动量震荡指标
    @staticmethod
    def awesome_oscillator(df):       # 震荡指标AO
    @staticmethod
    def ultimate_oscillator(df):      # 终极震荡指标
    @staticmethod
    def money_flow_index(df):         # 资金流量指数MFI
```

### 1.3 波动率指标
```python
class VolatilityIndicators:
    @staticmethod
    def atr(df, period=14):           # 平均真实波幅
    @staticmethod
    def keltner(df, period=20):       # 肯特纳通道
    @staticmethod
    def donchian(df, period=20):      # 唐奇安通道
    @staticmethod
    def std_dev(df, period=20):       # 标准差通道

    # 历史波动率
    @staticmethod
    def historical_volatility(df, period=30):
    @staticmethod
    def garman_klass(df):             # Garman-Klass波动率估计
```

### 1.4 成交量指标
```python
class VolumeIndicators:
    @staticmethod
    def obv(df):                      # 能量潮
    @staticmethod
    def ad_line(df):                  # 累积/派发线
    @staticmethod
    def chaikin_money_flow(df):       # 佳庆资金流量
    @staticmethod
    def force_index(df, period=13):   # 强力指数
    @staticmethod
    def volume_profile(df):           # 成交量分布
    @staticmethod
    def vwap(df):                     # 成交量加权平均价
    @staticmethod
    def ease_of_movement(df):         # 简易波动指标
```

### 1.5 市场广度指标
```python
class MarketBreadthIndicators:
    @staticmethod
    def advance_decline(df):          # 涨跌线AD
    @staticmethod
    def mcclellan_oscillator():       # 麦克连指标
    @staticmethod
    def new_highs_lows():             # 新高新低
    @staticmethod
    def put_call_ratio():             # 卖出/买入比率
```

### 1.6 形态识别
```python
class PatternRecognition:
    @staticmethod
    def detect_doji(df):              # 十字星
    @staticmethod
    def detect_hammer(df):            # 锤子线
    @staticmethod
    def detect_engulfing(df):         # 吞没形态
    @staticmethod
    def detect_harami(df):            # 孕线形态
    @staticmethod
    def detect_piercing(df):          # 刺透形态
    @staticmethod
    def detect_cloud_break(df):       # 云突破

    # K线形态组合
    @staticmethod
    def detect_morning_star(df):      # 早晨之星
    @staticmethod
    def detect_evening_star(df):      # 黄昏之星

    # 价格形态
    @staticmethod
    def detect_head_shoulders(df):    # 头肩顶/底
    @staticmethod
    def detect_double_top(df):        # 双顶/双底
    @staticmethod
    def detect_triangle(df):          # 三角形整理
    @staticmethod
    def detect_wedge(df):             # 楔形
```

---

## 二、高级量化算法

### 2.1 统计套利策略
```python
class StatisticalArbitrage:
    @staticmethod
    def pairs_trading(stock_a, stock_b):      # 配对交易
        # 协整检验
        # 价差计算
        # Z-score信号

    @staticmethod
    def cointegration_pairs(universe):        # 协整配对筛选
        # ADF检验
        # Johansen测试

    @staticmethod
    def mean_reversion(df):                   # 均值回归
        # Ornstein-Uhlenbeck过程
        # 半衰期计算
```

### 2.2 因子模型
```python
class FactorModels:
    @staticmethod
    def fama_french_three_factor(returns):    # Fama-French三因子
    @staticmethod
    def fama_french_five_factor(returns):    # Fama-French五因子
    @staticmethod
    def carhart_four_factor(returns):        # Carhart四因子

    # 多因子选股
    @staticmethod
    def multi_factor_score(stock_data):
        factors = {
            'value': [],      # 价值因子: PE, PB, PS, PCF
            'growth': [],     # 成长因子: 营收增长, 利润增长
            'quality': [],    # 质量因子: ROE, ROA, 毛利率
            'momentum': [],   # 动量因子: 过去3-12月收益
            'volatility': [], # 波动率因子: 标准差, 下行风险
            'size': [],       # 规模因子: 市值
        }

    @staticmethod
    def factor_rotation(factors, macro_data): # 因子轮动
```

### 2.3 机器学习预测
```python
class MLPrediction:
    @staticmethod
    def random_forest_predict(features, target):
        # 特征工程
        # 模型训练
        # 特征重要性

    @staticmethod
    def xgboost_predict(features, target):
        # XGBoost回归/分类
        # 参数调优
        # 早停机制

    @staticmethod
    def lstm_predict(time_series):
        # LSTM时间序列预测
        # 序列构建
        # 多步预测

    @staticmethod
    def transformer_predict(time_series):
        # Transformer架构
        # Attention机制
        # 位置编码
```

### 2.4 强化学习交易
```python
class RLTrading:
    """
    使用DQN/PPO/A3C等算法训练智能体
    """
    @staticmethod
    def dqn_trading(environment):
        # Deep Q-Network
        # 经验回放
        # 目标网络

    @staticmethod
    def ppo_trading(environment):
        # Proximal Policy Optimization
        # Actor-Critic架构
        # 策略梯度

    @staticmethod
    def a3c_trading(environment):
        # Asynchronous Advantage Actor-Critic
        # 多线程异步训练

    # 自定义交易环境
    class TradingEnv(gym.Env):
        def __init__(self, data, initial_balance=100000):
            # 状态: 持仓、现金、价格、技术指标
            # 动作: 买入、卖出、持有
            # 奖励: 收益率-交易成本-风险惩罚
```

### 2.5 组合优化
```python
class PortfolioOptimization:
    @staticmethod
    def markowitz_optimization(returns, risk_free_rate=0.03):
        # 均值-方差优化
        # 有效前沿

    @staticmethod
    def black_litterman(returns, views):
        # Black-Litterman模型
        # 投资观点融合

    @staticmethod
    def risk_parity(returns):
        # 风险平价
        # 等风险贡献

    @staticmethod
    def hierarchical_risk_parity(returns):
        # HRP层次风险平价
        # 聚类分析

    @staticmethod
    def cva_r_optimization(returns, confidence=0.95):
        # 条件VaR优化
        # CVaR风险度量

    @staticmethod
    def kelly_criterion(win_rate, avg_win, avg_loss):
        # 凯利公式仓位管理
```

### 2.6 信号生成与过滤
```python
class SignalGeneration:
    @staticmethod
    def multi_timeframe_signal(df_short, df_mid, df_long):
        # 多周期共振

    @staticmethod
    def signal_filtering(signals, filters):
        # 信号过滤
        filters = {
            'trend_filter': [],      # 趋势过滤
            'volatility_filter': [],  # 波动率过滤
            'correlation_filter': [], # 相关性过滤
            'volume_filter': [],      # 成交量确认
        }

    @staticmethod
    def signal_combination(signals, weights):
        # 信号融合
        # 投票机制
        # 加权组合

    @staticmethod
    def walk_forward_analysis(strategy, data, window=252):
        # 滚动窗口分析
        # 参数自适应
```

---

## 三、风险管理模块

### 3.1 风险度量
```python
class RiskMetrics:
    @staticmethod
    def var(returns, confidence=0.95, method='historical'):
        # VaR计算: 历史法/参数法/蒙特卡洛

    @staticmethod
    def cvar(returns, confidence=0.95):
        # 条件VaR/期望损失

    @staticmethod
    def max_drawdown(equity_curve):
        # 最大回撤
        # 回撤持续期

    @staticmethod
    def sharpe_ratio(returns, risk_free_rate=0.03):
        # 夏普比率

    @staticmethod
    def sortino_ratio(returns, risk_free_rate=0.03):
        # 索提诺比率

    @staticmethod
    def calmar_ratio(returns):
        # 卡玛比率

    @staticmethod
    def information_ratio(returns, benchmark_returns):
        # 信息比率

    @staticmethod
    def beta(returns, market_returns):
        # Beta系数

    @staticmethod
    def alpha(returns, market_returns, risk_free_rate=0.03):
        # Alpha
```

### 3.2 仓位管理
```python
class PositionManagement:
    @staticmethod
    def equal_weight(capital, n_stocks):
        # 等权重分配

    @staticmethod
    def risk_parity_position(cov_matrix, capital):
        # 风险平价仓位

    @staticmethod
    def kelly_position(win_rate, odds, capital):
        # 凯利公式仓位

    @staticmethod
    def fixed_fractional(capital, risk_per_trade, stop_loss):
        # 固定比例

    @staticmethod
    def volatility_based_position(capital, volatility, target_vol):
        # 基于波动率的仓位

    @staticmethod
    def atr_based_position(price, atr, risk_amount):
        # 基于ATR的仓位
```

---

## 四、回测框架

### 4.1 事件驱动回测
```python
class BacktestEngine:
    def __init__(self):
        self.data = None
        self.strategy = None
        self.broker = None

    def add_data(self, data):
        # 添加历史数据

    def add_strategy(self, strategy):
        # 添加交易策略

    def set_broker(self, initial_cash, commission):
        # 设置券商参数

    def run(self):
        # 运行回测
        # 返回: 交易记录、资金曲线、绩效指标

class Performance:
    @staticmethod
    def calculate_all_metrics(trades, equity_curve):
        return {
            'total_return': [],        # 总收益率
            'annual_return': [],       # 年化收益
            'sharpe_ratio': [],        # 夏普比率
            'max_drawdown': [],        # 最大回撤
            'win_rate': [],            # 胜率
            'profit_factor': [],       # 盈亏比
            'avg_trade': [],           # 平均交易收益
            'trade_count': [],         # 交易次数
        }
```

### 4.2 参数优化
```python
class ParameterOptimization:
    @staticmethod
    def grid_search(strategy, param_grid, data):
        # 网格搜索

    @staticmethod
    def random_search(strategy, param_distributions, n_iter=100):
        # 随机搜索

    @staticmethod
    def bayesian_optimization(strategy, param_bounds, n_iter=50):
        # 贝叶斯优化

    @staticmethod
    def genetic_algorithm(strategy, params, generations=50):
        # 遗传算法优化

    @staticmethod
    def walk_forward_optimization(strategy, data, train_period, test_period):
        # 滚动窗口优化
```

---

## 五、实时交易系统

### 5.1 行情监控
```python
class LiveMarketMonitor:
    def __init__(self, symbols):
        self.symbols = symbols
        self.callbacks = []

    def add_callback(self, callback):
        # 添加价格更新回调

    def on_bar(self, bar):
        # K线更新触发

    def on_tick(self, tick):
        # 逐笔成交触发

    def start(self):
        # 启动监控
```

### 5.2 订单管理
```python
class OrderManager:
    def create_order(self, symbol, direction, quantity, order_type='market'):
        # 创建订单

    def limit_order(self, symbol, price, quantity):
        # 限价单

    def stop_order(self, symbol, stop_price, quantity):
        # 止损单

    def iceberg_order(self, symbol, price, total_qty, display_qty):
        # 冰山单

    def twap_order(self, symbol, total_qty, duration):
        # TWAP算法拆单

    def vwap_order(self, symbol, total_qty, duration):
        # VWAP算法拆单
```

### 5.3 风险控制
```python
class RiskControl:
    def __init__(self, max_position=0.3, max_loss_per_trade=0.02):
        self.max_position = max_position        # 单股最大仓位
        self.max_loss_per_trade = max_loss_per_trade  # 单笔最大亏损

    def check_position_limit(self, symbol, quantity, price):
        # 检查仓位限制

    def check_loss_limit(self, entry_price, current_price):
        # 检查止损

    def check_portfolio_risk(self, positions):
        # 检查组合风险

    def daily_loss_limit(self, realized_pnl, limit=0.05):
        # 日亏损限制
```

---

## 六、实现优先级

### 第一阶段 (1-2周)
1. 扩展技术指标库
   - [ ] ATR, KDJ, CCI, Williams %R
   - [ ] OBV, 成交量相关指标
   - [ ] 支撑压力位自动计算

2. 完善回测框架
   - [ ] 标准化回测接口
   - [ ] 绩效指标计算
   - [ ] 交易记录保存

### 第二阶段 (2-4周)
3. 统计套利模块
   - [ ] 配对交易
   - [ ] 协整检验

4. 因子选股系统
   - [ ] 多因子计算
   - [ ] 因子有效性测试
   - [ ] 因子组合优化

### 第三阶段 (4-8周)
5. 机器学习模块
   - [ ] 特征工程
   - [ ] Random Forest/XGBoost预测
   - [ ] 特征重要性分析

6. 风险管理模块
   - [ ] VaR/CVaR计算
   - [ ] 仓位管理系统
   - [ ] 止损止盈策略

### 第四阶段 (长期)
7. 深度学习
   - [ ] LSTM时间序列预测
   - [ ] Transformer模型
   - [ ] 强化学习交易

8. 实盘系统
   - [ ] 券商接口对接
   - [ ] 实时信号推送
   - [ ] 自动交易执行

---

## 七、技术栈建议

```bash
# 核心依赖
pip install akshare pandas numpy

# 技术分析
pip install ta-lib ta

# 机器学习
pip install scikit-learn xgboost lightgbm

# 深度学习
pip install tensorflow pytorch

# 回测框架
pip install backtrader zipline

# 数据可视化
pip install matplotlib plotly seaborn

# 统计分析
pip install statsmodels scipy

# 数据库
pip install sqlalchemy pymongo

# Web框架(可选)
pip install fastapi streamlit
```

---

## 八、学习资源推荐

### 书籍
1. 《量化投资：策略与技术》- 丁鹏
2. 《打开量化投资的黑箱》- 里什·纳兰
3. 《算法交易》- 欧内斯特·陈
4. 《Python金融大数据分析》- 希尔皮斯科

### 论文
1. Fama, E. F., & French, K. R. (2015). A five-factor asset pricing model
2. Jegadeesh, N., & Titman, S. (1993). Returns to buying winners and selling losers
3. Pástor, Ľ., & Stambaugh, R. F. (2003). Liquidity risk and expected stock returns

### 在线资源
1. Quantopian Lectures (已归档，但资料仍在)
2. QuantConnect文档
3. ARQ (Alpha Research Platform)

---

*更新日期: 2026-02-03*
